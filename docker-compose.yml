services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    ports:
      - "80:80"
